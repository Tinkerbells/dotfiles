#!/bin/bash

wget --spider --quiet http://google.com

network=$(nmcli -o c | awk '{print $1;}' | sed -n '2 p')

if [ "$?" != 0 ]; then
  #not connected
  echo "睊  disconnected"
else
  #connected
  echo "直  $network"
fi

case $BLOCK_BUTTON in
  1) alacritty -e nmtui ;;
esac
