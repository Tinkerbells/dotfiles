#!/bin/sh

setxkbmap -query | grep -q 'us' && (
  dunstify "RU" -t 1000 -i layout -r 999 &
  setxkbmap ru
) || (
  dunstify "US" -t 1000 -i layout -r 999 &
  setxkbmap us
) && kill -43 $(pidof dwmblocks)
